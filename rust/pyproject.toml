[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "feetbrowser-engine"
version = "0.1.0"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Rust",
]

[tool.maturin]
module-name = "feetbrowser_engine"
features = ["pyo3/extension-module"]